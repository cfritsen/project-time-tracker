import React from 'react'
import TimeCard from './TimeCard'


export default function TimeCardList(props) {
    const {timeEntry, setTimeEntry} = props;

    return(
        <div class='timecard-list'>

        </div>
    )
}